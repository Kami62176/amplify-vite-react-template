import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#2b2b3d'
        },
        secondary: {
            main: '#1A1A28'
        }
    }
})

export default theme